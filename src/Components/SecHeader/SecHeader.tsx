
const SecHeader = ({ title }: { title: string }) => {
  return (
    <div className="text-2xl/[32px] font-semibold text-gray-2 dark:text-white mb-8">
      {title}
    </div>
  );
};

export default SecHeader
